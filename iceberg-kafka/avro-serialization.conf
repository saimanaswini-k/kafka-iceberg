# Use Avro serialization where possible
pipeline.serializers: AVRO

# Enable object reuse for better performance
pipeline.object-reuse: true

# Allow generic types (needed for RowData)
pipeline.generic-types.enabled: true 